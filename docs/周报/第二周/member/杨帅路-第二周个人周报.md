# 第二周个人周报 —— 杨帅路

**时间**：2026年7月13日 — 7月17日

**小组分工角色**：可视化交互界面与专业量化回测体系

* * *

## 本周完成工作

本周是项目编码启动周，本人按照分工安排，重点完成**可视化交互界面搭建**与**专业量化回测体系编排**，并承担算法正确性验证与真实数据冒烟测试，协同打通「滚动训练 → 模型落盘 → 预测 → 回测 → Web 展示」全流程闭环。

1. **可视化交互界面（Web 前端）**
   完成 `config.html` 参数配置页与 `result.html` 回测结果页，配套 `result.js` 实现结果可视化与交互；通过 `PageController` / `ApiController` 完成前后端接口对接。回测结果页覆盖绩效指标卡片、K 线、资金曲线、回撤曲线、多指标信号曲线、年度收益表与交易明细，并支持已训练模型的选择复用，把第一周的原型落地为可运行界面。

2. **专业量化回测体系**
   完成分析编排服务，统一串起全流程：`train()` 跑滚动窗口 GA 训练并把最后一个窗口最优染色体落盘为模型；复用训练阶段样本外信号，对优化策略与固定策略做对照回测； 支持用已落盘模型直接回测、无需重训。

3. **批量训练与数据接入**
   实现 `trainFolder()` 文件夹批量训练：递归遍历目录下全部 CSV（每个视为一个标的）逐个训练落盘，单个标的失败不中断整体。

* * *

## 遇到的问题

- 模型落盘后重新加载预测时，染色体数组需与训练时指标顺序严格对齐，否则信号错位；已用模型保存的指标名列表重建指标集并通过往返一致性测试解决。
- 批量训练中多个标的同名 `1d.csv` 在落盘时冲突，已按所在子目录名命名标的区分。

## 下周计划

1. 根据联调反馈优化性能与交互体验，完善页面异常处理与边界提示。
2. 校验回测结果合理性，补充测试用例，保持文档与实现一致。
3. 配合组员进行适应度函数与训练模型优化。
