# 配置与运行文档 · 基于遗传算法的量化策略分析系统

> GA RollTrade —— 遗传算法优化量化交易策略，五维盈亏综合适应度驱动，滚动 3/1/1 窗口训练 + 外推回测。

---

## 一、环境要求

| 依赖 | 版本 | 说明 |
|------|------|------|
| JDK | 17 | 项目 `pom.xml` 指定 `java.version=17`，需用 JDK 17 |
| Maven | 3.6+ | 构建/运行（3.9.x 已验证） |
| 浏览器 | 任意现代浏览器 | 访问前端页面 |
| OS | Windows / macOS / Linux | 开发在 Windows 11 验证 |

启动后默认监听 **`http://localhost:8080`**（见 `src/main/resources/application.properties`）。

---

## 二、安装与启动

### 方式 A：Maven 命令行（推荐）

在项目根目录执行：

```bash
mvn clean package
mvn spring-boot:run
```

看到 `Started ... in X seconds` 即启动成功，浏览器打开 `http://localhost:8080`。

### 方式 B：IDE

用 IntelliJ IDEA / VS Code 打开项目，运行主类（带 `@SpringBootApplication` 的启动类）即可。

### 方式 C：打包后运行

```bash
mvn clean package -DskipTests
java -jar target/ga-rolltrade-pred-0.0.1-SNAPSHOT.jar
```

> 首次构建 Maven 会下载依赖，需要联网。

---

## 三、数据格式要求

系统读取 **CSV 行情数据**，首行作为表头。必填字段与可选别名如下（中英文均可）：

| 字段 | 别名（任选其一） | 必填 |
|------|------------------|------|
| 日期/时间 | `date` / `time` / `datetime` / `日期` / `时间` | ✅ |
| 开盘价 | `open` / `开盘价` / `开盘` | ✅ |
| 最高价 | `high` / `最高价` / `最高` | ✅ |
| 最低价 | `low` / `最低价` / `最低` | ✅ |
| 收盘价 | `close` / `收盘价` / `收盘` | ✅ |
| 成交量 | `volume` / `vol` / `成交量` / `手数` | ❌（缺失按 0） |

**日期格式**支持：
- 时间戳：10 位（秒）/ 13 位（毫秒），如 `1659456000` / `1659456000000`
- 字符串：`yyyy-MM-dd`、`yyyy/MM/dd`、`yyyy-MM-dd HH:mm:ss`、`yyyy/MM/dd HH:mm:ss`、`yyyyMMdd`

**约束**：数据必须**按时间升序**排列，否则报错。价格为正、成交量为非负。

**示例**（`src/main/resources/static/sample/中证1000/159629.SZ/1d.csv` 的表头）：
```
time,open,high,low,close,volume,amount,settelementPrice,openInterest,preClose,suspendFlag
1659283200000,1.012,1.018,1.009,1.015,123456,...
```
其中多余的 `amount`、`openInterest` 等列会被忽略，只要含上表必填列即可。

---

## 四、前端页面使用流程（图形界面）

左侧导航四个页面，推荐顺序：

### 1. 首页 `/`
显示当前是否有数据、是否有回测结果。

### 2. 数据管理 `/data`
- 点击「加载示例」使用内置示例数据，或
- 点击「上传 CSV」上传自己的行情 CSV
- 上传成功后显示数据预览（首末日期、K 线数、价格区间）

### 3. 参数配置 `/config`
- **勾选技术指标**（9 选 N）：MA / MACD / RSI / BOLL / KDJ / ATR / OBV / CCI / MOM
- 调整 GA 参数与滚动窗口（详见第六节参数说明，一般保持默认即可）
- 点击「开始回测」→ 跳转到结果页

### 4. 回测结果 `/result`
展示（基于样本外数据的回测）：
- 绩效指标卡：累计收益、年化收益、最大回撤、夏普、胜率、盈亏比、交易次数、买入持有
- 价格走势与买卖点（散点标在 K 线上）
- **技术指标信号曲线**（多指标同步，信号分 -1~1）
- 资金曲线对比（优化策略 vs 固定策略）
- 回撤曲线
- **回撤幅度分布统计**（分桶直方图）
- 策略对比表、**年度收益统计表**、交易明细、滚动窗口区间表

---

## 五、REST API 接口

所有接口前缀 `/api`，JSON 交互。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/upload` | 上传 CSV（`multipart/form-data`，字段 `file`），返回数据预览 |
| GET | `/api/sample` | 加载内置示例数据 |
| GET | `/api/preview` | 查看当前数据预览 |
| GET | `/api/indicators` | 返回全部可用指标名 |
| POST | `/api/train` | 用当前数据训练择时模型并落盘，body=`StrategyConfig` |
| GET | `/api/model` | 查看最近一次训练摘要 |
| POST | `/api/predict?model=<数据名>` | 加载已训练模型对当前数据逐根预测 |
| GET | `/api/predict` | 查看最近一次预测结果 |
| POST | `/api/train-folder?path=<文件夹>` | 批量训练文件夹内所有 CSV，body 可选 `StrategyConfig` |
| GET | `/api/train-folder` | 查看最近一次批量训练结果 |
| POST | `/run` | 前端表单提交：训练 + 回测，跳转结果页 |
| GET | `/api/result` | 获取最近一次回测报告（结果页数据源） |

### 典型调用链

**单标的回测看图**：上传 → `/api/result`（或前端走 `/config` → `/run` → `/result`）

**单标的训练 + 预测**：上传 CSV → `POST /api/train` → 上传要预测的数据 → `POST /api/predict?model=<数据名>`

**批量训练**：`POST /api/train-folder?path=<文件夹>`（详见第八节）

---

## 六、策略配置参数说明（`StrategyConfig`）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `indicators` | 全部 | 启用的指标名列表，未指定=启用全部 9 个 |
| `populationSize` | 80 | GA 种群规模 |
| `generations` | 120 | GA 迭代代数 |
| `crossoverRate` | 0.78 | 交叉概率 |
| `mutationRate` | 0.08 | 变异概率 |
| `trainMonths` | 3 | 训练窗口长度（月） |
| `predictMonths` | 1 | 预测窗口长度（月，样本外） |
| `stepMonths` | 1 | 滚动步长（月） |
| `initialCapital` | 1,000,000 | 初始资金 |
| `commissionRate` | 0.0003 | 手续费率（万三） |
| `forecastDays` | 5 | 方向标签 horizon（辅助可验证性用） |
| `labelThreshold` | 0.0 | 涨跌中性带阈值 |
| `wReturn` | 0.30 | **五维适应度权重**：年化收益 |
| `wDrawdown` | 0.25 | 最大回撤 |
| `wSharpe` | 0.20 | 夏普比率 |
| `wWinRate` | 0.10 | 胜率 |
| `wProfitLoss` | 0.15 | 盈亏比 |

> 五个 `w*` 权重**驱动 GA 训练适应度**；`wAccuracy`/`wReturnWeighted` 为辅助可验证性权重，**不驱动训练**。五维权重之和无需为 1，按相对大小加权。

---

## 七、核心算法说明

- **染色体 4D 编码**：指标掩码 + 参数候选索引 + 指标权重 + 买入/卖出阈值。
- **适应度**：染色体生成信号 → 在训练段跑迷你回测 → 五维盈亏（年化收益 + (1-最大回撤) + 夏普 + 胜率 + 盈亏比）归一化加权。无完整交易 → 惩罚分 -0.5。
- **滚动窗口**：3 月训练 / 1 月预测 / 1 月步长，**严格时序隔离**（预测窗口始终晚于训练窗口，杜绝未来数据泄露）。
- **GA 算子**：锦标赛选择 + 均匀交叉 + 变异 + 精英保留。
- **最终模型**：取最后一个滚动窗口的最优染色体落盘，覆盖最新行情段。
- **方向预测准确率**保留为辅助可验证性指标，不参与训练驱动。

---

## 八、大量数据批量训练

支持递归遍历文件夹，每个 CSV（一个标的）独立训练、独立落盘。

### 数据布局

```
中证1000/
├── 159629.SZ/1d.csv
├── 159633.SZ/1d.csv
└── ...
```
标的名取子目录名（`159629.SZ`），避免多个同名 `1d.csv` 在落盘时冲突。

### 命令（Windows PowerShell）

> ⚠️ PowerShell 里 `curl` 是 `Invoke-WebRequest` 别名，不认 `-H`/`-d`，**必须用 `curl.exe`**，且**写成一行**、**中文路径要百分号编码**（Tomcat 拒绝非 ASCII 字符）。

`中证1000` 编码后为 `%E4%B8%AD%E8%AF%811000`（`中=%E4%B8%AD`、`证=%E8%AF%81`）。

**批量训练全部标的**（默认参数，种群 80 迭代 120）：

```powershell
curl.exe -X POST -H "Content-Type: application/json" -d "{}" "http://localhost:8080/api/train-folder?path=C:/Users/34173/Desktop/基于遗传算法的量化策略分析系统/src/main/resources/static/sample/%E4%B8%AD%E8%AF%811000"
```

**加速训练**（调小 GA 参数，约快 6 倍）：

```powershell
curl.exe -X POST -H "Content-Type: application/json" -d "{\"populationSize\":40,\"generations\":40}" "http://localhost:8080/api/train-folder?path=C:/Users/34173/Desktop/基于遗传算法的量化策略分析系统/src/main/resources/static/sample/%E4%B8%AD%E8%AF%811000"
```

**先训练单个标的验证**（路径指到单个子目录）：

```powershell
curl.exe -X POST -H "Content-Type: application/json" -d "{}" "http://localhost:8080/api/train-folder?path=C:/Users/34173/Desktop/基于遗传算法的量化策略分析系统/src/main/resources/static/sample/%E4%B8%AD%E8%AF%811000/159629.SZ"
```

### 命令（Linux / macOS / Git Bash）

可用 `\` 换行：

```bash
curl -X POST -H "Content-Type: application/json" -d "{}" \
  "http://localhost:8080/api/train-folder?path=/path/to/中证1000"
```
（Bash 下中文一般无需手动编码。）

### 训练产物与进度

- 每个标的训练完落盘：`models/<标的名>-timing-model.json`
- 单个标的失败（数据不足/格式错误）**不中断整体**，记为失败项返回。
- `train-folder` 是**同步接口**，数据多时会阻塞到全部完成。即便请求超时，已训练完的模型也已落盘——看 `models/` 目录文件数即可判断进度：
  ```powershell
  (Get-ChildItem models\*.json).Count
  ```
- 数据量大时建议：**分批**（路径指到单个子目录逐个跑），或**调小 GA 参数**，或**后台运行**。

### 训练后预测某标的

```powershell
# 1) 上传该标的数据
curl.exe -F "file=@C:/Users/34173/Desktop/基于遗传算法的量化策略分析系统/src/main/resources/static/sample/中证1000/159629.SZ/1d.csv" http://localhost:8080/api/upload
# 2) 用该标的的模型预测（model=标的名）
curl.exe -X POST "http://localhost:8080/api/predict?model=159629.SZ"
```

---

## 九、模型与产物

| 产物 | 位置 | 说明 |
|------|------|------|
| 训练模型 | `models/<数据名>-timing-model.json` | 最后一个滚动窗口的最优染色体，含指标名顺序、阈值、适应度 |
| 回测报告 | 内存（`/api/result`） | 重启后清空，需重新回测 |

模型文件名安全化：标的名中非 `[A-Za-z0-9._-]` 字符替换为 `_`，`.csv` 后缀去除。如 `159629.SZ` → `159629.SZ-timing-model.json`。

---

## 十、测试

```bash
mvn test
```
覆盖：指标手算正确性、信号评分范围、回测资金守恒、滚动窗口时序隔离、GA 染色体合法性、五维盈亏无交易惩罚、模型往返一致性、动量指标注册等。

---

## 十一、常见问题

**Q1: PowerShell 报「无法绑定参数 Headers」/「400 错误的请求」**
A: PowerShell 的 `curl` 是 `Invoke-WebRequest` 别名。改用 `curl.exe`，整条写一行，中文路径做百分号编码（见第八节）。

**Q2: 上传 CSV 报「时间字段无法解析」**
A: 检查日期列格式是否在支持列表内（第三节）。时间戳必须 10 位（秒）或 13 位（毫秒）纯数字。

**Q3: 报「数据未按时间升序排列」**
A: CSV 必须按日期从早到晚排序后上传。

**Q4: 回测显示「无交易记录」/ 适应度 -0.5**
A: 当前染色体在该段未产生完整买卖。可调整指标组合、放宽阈值，或换更长数据段。

**Q5: `train-folder` 返回 400 / 找不到 CSV**
A: `path` 必须指向**包含 CSV 的文件夹**，且用正斜杠 `/`。中文必须百分号编码。文件夹内至少有一个 `.csv`。

**Q6: 端口 8080 被占用**
A: 修改 `application.properties` 的 `server.port`。

**Q7: 模型名冲突 / 多个 1d.csv 互相覆盖**
A: 用 `/api/train-folder`（按子目录命名），不要用 `/api/train` 训练多个同名 `1d.csv`（后者按上传文件名 `1d.csv` 落盘会互相覆盖）。

---

## 十二、项目结构速览

```
src/main/java/com/example/quant/
├── controller/      PageController(页面) + ApiController(REST)
├── data/            CsvLoader / DataValidator(解析与校验)
├── indicator/       IndicatorPool + impl/(9 个指标)
├── ga/              GeneticAlgorithm / FitnessEvaluator(五维盈亏)
├── rolling/         RollingWindowService(3/1/1 滚动)
├── backtest/        BacktestEngine / MetricsCalculator
├── strategy/        SignalGenerator / FixedStrategy
├── service/         AnalysisService(编排) / DataService / ModelRepository
└── model/           数据模型(Chromosome/KLine/StrategyConfig/TrainedModel...)
src/main/resources/
├── application.properties
├── templates/        Thymeleaf 页面(index/data/config/result)
├── static/js,css/    前端资源(ECharts 5.5.0)
└── static/sample/    示例数据(中证1000/<标的>/1d.csv)
```
